services:
  db:
    # Placeholder, actual DB chosen in override
    networks:
      default:
        aliases:
          - db

  app:
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    env_file:
      - ../.env
    ports:
      - "8080:8080"
    depends_on:
      - db

networks:
  default:
    name: mynet